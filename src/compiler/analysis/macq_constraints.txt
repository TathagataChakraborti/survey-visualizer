; Use the <type>:<description> pattern for custom constraints
; <description> should be a comma-separated list of feature names
; <type> can be one of:
;   - atmostone
;   - atleastone
;   - oneof
;   - implies
atmostone:Rationality
implies:Uncertainty > Probabilistic,Uncertainty > Non-deterministic
implies:Uncertainty > Non-deterministic,Uncertainty > Deterministic
